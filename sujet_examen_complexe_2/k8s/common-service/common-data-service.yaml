apiVersion: v1
kind: Service
metadata:
  name: common-data-service
  namespace: ecommerce-project
spec:
  selector:
    app: common-data
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000