apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ecommerce-project
spec:
  selector:
    app: auth
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 7000
      targetPort: 7000